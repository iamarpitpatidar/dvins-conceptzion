{
  "name": "dvins-conceptzion",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pug *.pug -w --out dist",
    "build": "pug *.pug --out dist"
  },
  "devDependencies": {
    "pug-cli": "^1.0.0-alpha6"
  }
}
